services:
  db:
    image: postgres:16
    container_name: online_cinema
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_theater_data:/var/lib/postgresql/data/

volumes:
  postgres_theater_data:
